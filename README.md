# Тестирование производительности REST API: виды тестов и ключевые метрики
Оглавление
Введение

Виды тестирования производительности REST-API  
2.1. Нагрузочное тестирование  
2.2. Стрессовое тестирование  
2.3. Объёмное тестирование

Метрики производительности REST API  
3.1. Время отклика  
3.2. Пропускная способность  
3.3. Уровень ошибок  
3.4. Использование ресурсов  
3.5. Latency  
3.6. Concurrency

Заключение

Список литературы

Введение
REST API является фундаментальным компонентом современных распределённых систем, обеспечивая взаимодействие между сервисами, клиентскими приложениями и внешними интеграциями. Производительность API напрямую влияет на пользовательский опыт, масштабируемость системы и её способность выдерживать реальные нагрузки. Поэтому тестирование производительности REST API является обязательным этапом обеспечения качества.

1. Виды тестирования производительности REST API
1.1. Нагрузочное тестирование
Нагрузочное тестирование (Load Testing) направлено на проверку работы API при нагрузке, которая считается нормальной или близкой к ожидаемой в реальной эксплуатации. Его цель — убедиться, что система стабильно функционирует в пределах проектных значений.

1.1.1. Что обычно проверяют
стабильность времени отклика при росте числа запросов;

способность API выдерживать длительную нагрузку без деградации;

корректность работы под типичным трафиком (например, 500 RPS в течение часа);

соответствие SLA, если оно определено.

1.1.2. Типичные проблемы
постепенное увеличение времени отклика;

утечки памяти;

рост очередей в базе данных;

неэффективные запросы, проявляющиеся только под нагрузкой.

1.1.3. Пример
API интернет-магазина должен выдерживать 2000 запросов в минуту в течение рабочего дня. Нагрузочный тест имитирует такой трафик, чтобы убедиться, что система не теряет производительность к концу дня.

1.2. Стрессовое тестирование
Стрессовое тестирование (Stress Testing) используется для определения предельных возможностей системы. В отличие от нагрузочного тестирования, здесь нагрузка намеренно превышает нормальные значения.

1.2.1. Цели стресс-тестирования
определить точку отказа API;

понять поведение системы при перегрузке;

проверить способность к восстановлению после отказа;

выявить узкие места, не проявляющиеся при обычной нагрузке.

1.2.2. Что важно учитывать
не только момент отказа, но и поведение после него;

корректность обработки ошибок (например, возврат 503 вместо зависания);

способность API восстановиться без ручного вмешательства.

1.2.3. Пример
Если API рассчитан на 1000 RPS, стресс-тест может довести нагрузку до 5000 RPS, чтобы определить момент деградации и скорость восстановления.

1.3. Объёмное тестирование
Объёмное тестирование (Volume Testing) проверяет работу API при больших объёмах данных. Оно важно для систем, работающих с крупными JSON-структурами, файлами или большими выборками из базы данных.

1.3.1. Что проверяют
время обработки больших payload;

влияние размера данных на время отклика;

устойчивость к большим ответам (например, 10 МБ JSON);

корректность работы при больших объёмах данных в базе.

1.3.2. Типичные проблемы
переполнение памяти при сериализации больших объектов;

медленные запросы к базе данных;

увеличение времени сборки мусора (GC);

ошибки при обработке крупных файлов.

1.3.3. Пример
API аналитической системы должен возвращать отчёты размером до 20 МБ. Объёмный тест проверяет, как API ведёт себя при таких данных и не приводит ли это к перегрузке сервера.

2. Метрики производительности REST API
Метрики позволяют объективно оценить состояние системы и выявить узкие места. Ниже приведены ключевые показатели, используемые в инженерной практике.

2.1. Время отклика (Response Time)
Показывает, сколько времени проходит между отправкой запроса и получением ответа.

Варианты измерений
среднее время отклика;

медиана;

95-й и 99-й перцентили;

максимальное время отклика.

Среднее значение может быть низким, но если небольшая часть запросов отвечает слишком долго, это приводит к ухудшению пользовательского опыта. Поэтому перцентили считаются более показательными.

2.2. Пропускная способность (Throughput)
Количество запросов, которое API способен обработать за единицу времени (обычно RPS).

Что важно
стабильность пропускной способности при росте нагрузки;

зависимость RPS от размера данных;

способность API сохранять производительность при длительных нагрузках.

2.3. Уровень ошибок (Error Rate)
Процент запросов, завершившихся ошибкой (4xx, 5xx).

Что анализируют
рост ошибок при увеличении нагрузки;

типы ошибок (например, 500 — внутренняя ошибка сервера, 429 — превышение лимитов);

связь ошибок с временем отклика и загрузкой ресурсов.

2.4. Использование ресурсов (CPU / Memory / Disk / Network)
Мониторинг ресурсов является обязательной частью нагрузочных тестов.

Что важно
пики загрузки CPU при сериализации JSON;

рост потребления памяти при больших объёмах данных;

задержки диска при работе с базой данных;

сетевые задержки при передаче больших ответов.

2.5. Latency (задержка)
Время, которое запрос проводит в очереди до начала обработки. Особенно важно для систем с балансировщиками, очередями и API Gateway.

2.6. Concurrency (число одновременных соединений)
Показывает, сколько клиентов API может обслуживать одновременно без деградации производительности.

Заключение
Тестирование производительности REST API является важной частью обеспечения качества современных систем. Нагрузочные тесты позволяют убедиться, что система выдерживает ожидаемый трафик. Стрессовые тесты помогают определить пределы и подготовиться к пиковым ситуациям. Объёмные тесты выявляют проблемы, связанные с большими данными.

Использование метрик — от времени отклика до уровня ошибок и загрузки ресурсов — позволяет принимать инженерные решения на основе объективных данных и поддерживать стабильность системы.
